# 보안 정책

## 취약점 제보

취약점을 발견하셨다면 **공개 이슈로 올리지 말아 주세요.** 아직 고쳐지지 않은 취약점이 공개되면 이 SDK를 쓰는 모든 프로젝트가 함께 노출됩니다.

두 가지 경로가 있습니다.

1. **GitHub 비공개 제보** (권장) — 이 저장소의 **Security** 탭 → **Report a vulnerability**. 제보 내용은 저장소 관리자에게만 보입니다.
2. **이메일** — security@bemyfriends.com

다음 내용이 있으면 확인이 빨라집니다.

- 영향받는 패키지와 버전
- 재현 절차 또는 개념 증명(PoC)
- 공격자가 무엇을 할 수 있게 되는지

## 응답

접수되면 확인 후 회신드립니다. 영향 판단과 수정 일정은 사안에 따라 다르며, 진행 상황을 제보 경로로 알려드립니다.

수정이 배포되기 전까지는 제보 내용을 공개하지 않습니다. 수정 후 제보자가 원하면 릴리즈 노트에 기여를 밝힙니다.

## 지원 버전

각 패키지는 독립 버전으로 배포되며, **최신 마이너 버전만** 보안 수정을 받습니다. 이전 버전을 쓰고 계시다면 먼저 최신 버전으로 올린 뒤 재현되는지 확인해 주세요. 버전 사이 변경점은 [docs/MIGRATION.md](docs/MIGRATION.md)에 있습니다.

## 범위

이 정책은 이 저장소가 배포하는 npm 패키지(`@bstage-sdk/*`)에 적용됩니다.

**범위 밖**입니다.

- b.stage 플랫폼 서비스 자체의 취약점 — 이 저장소가 아니라 서비스 쪽 제보 경로를 이용해 주세요
- 이 SDK로 만든 서드파티 템플릿의 취약점 — 해당 템플릿 개발자에게 문의해 주세요
- 로컬 개발 서버(`bstage dev`)의 설정을 개발자 본인이 바꿔서 생기는 문제

## 알아두실 것

- **템플릿은 서로 JavaScript 컨텍스트를 공유합니다.** 템플릿은 Shadow DOM에 마운트되어 **스타일**은 격리되지만 JavaScript는 격리되지 않습니다. 같은 페이지에 올라간 다른 템플릿이 전역을 덮어쓸 수 있습니다. 플랫폼은 템플릿 등록 심사로 이를 통제합니다.
- **`bstage dev`는 로컬 개발 전용입니다.** 인증 프록시가 쿠키의 보안 속성을 제거하고 토큰을 localhost에 저장하므로, 신뢰할 수 없는 네트워크에서 실행하지 마세요. 프로덕션 용도로 쓰도록 만들어지지 않았습니다.
