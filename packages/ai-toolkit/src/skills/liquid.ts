/**
 * liquid 테마 프로젝트에서 템플릿을 작성·수정하는 스킬.
 * `.claude/skills/bstage-liquid/SKILL.md`로 기록된다.
 *
 * 구조 규약(경로 깊이·이름 규칙)은 포털 빌더가 강제하는 것이라 여기 적는다.
 * liquid 문법 자체는 요점만 적고 상세는 liquidjs 문서로 넘긴다 — SDK가 소유한 지식이 아니다.
 */
export function liquidSkillMd(): string {
  return `---
name: bstage-liquid
description: b.stage liquid 템플릿을 작성·수정한다. liquid 레포에서 "liquid 템플릿", "template.liquid", "data.json", "페이지 하나 더" 같은 요청을 받을 때 사용한다.
---

# 역할

이 프로젝트에서 \`.liquid\` 템플릿을 작성한다. 작성한 템플릿은 포털이 그대로 패키징하고, 플랫폼이 **서버에서 데이터를 넣어** 렌더한다. 브라우저에서 도는 빌드 산출물이 아니다.

이 스킬은 구조 규약·절차·함정만 담는다. 문법 상세는 liquidjs 문서를, 명령 옵션은 \`bstage <명령> --help\`를 본다.

# 권위 있는 출처

- \`bstage --help\`, \`bstage <명령> --help\` — 명령·옵션
- 프로젝트 규칙: 레포 루트의 \`AGENTS.md\`
- 문법: liquidjs 공식 문서(태그·필터 목록). 여기 옮겨 적지 않는다
- 배포: \`bstage-deploy\` 스킬

# 플랫폼이 넣는 데이터 (유저 화면)

유저 화면 템플릿이 받는 최상위 값은 **다섯 개뿐**이다. 이 이름 외의 변수는 빈 문자열이 된다.

| 이름 | 내용 |
| --- | --- |
| \`lounges\` | 라운지 목록 |
| \`stories\` | 라운지별 스타 홈 피드를 모은 목록 |
| \`contentSections\` | 콘텐츠 홈 섹션 목록 |
| \`latestContents\` | 각 섹션의 콘텐츠를 발행일 내림차순으로 모은 목록 |
| \`shopCategories\` | 샵 카테고리 목록(대표 컬렉션 상품 포함) |

각 항목의 필드는 플랫폼 응답을 그대로 따르므로 **실제 렌더 결과로 확인한다.** 필드 이름을 기억으로 지어내지 않는다.

\`data.json\`에는 이 다섯 개와 **같은 이름**으로 샘플을 넣어야 로컬 프리뷰가 배포와 같은 모양이 된다. 다른 이름을 쓰면 로컬에서만 보이고 배포하면 비는데, 그 차이가 화면에 드러나지 않아 찾기 어렵다.

어드민 화면이 무엇을 받는지는 **확인되지 않았다.** 어드민 템플릿에서는 값이 온다고 가정하지 말고 첫 배포에서 실제 렌더를 보고 판단한다.

# 구조 규약 (포털 빌더가 강제한다)

\`\`\`
public/
  user/                      — 유저 플랫폼용
    {name}/
      template.liquid        — 실제 템플릿 (필수)
      data.json              — 로컬 미리보기용 샘플 데이터 (선택)
  admin/                     — 어드민용 (같은 규칙)
\`\`\`

- **깊이가 고정이다.** \`public/{user|admin}/{name}/template.liquid\` 정확히 이 자리만 인식한다. 한 단계 더 들어가거나 덜 들어가면 **포털이 조용히 무시한다**(빌드는 성공하는데 아무것도 안 나온다).
- \`{name}\`은 **소문자·숫자·하이픈**만. 대문자·밑줄·공백은 쓰지 않는다.
- 파일 이름은 반드시 \`template.liquid\`다.
- 한 폴더가 한 페이지다. 페이지를 더 만들려면 폴더를 더 만든다.

# 절차

## 1. 새 템플릿 추가

1. \`public/user/{name}/\` 폴더를 만든다(어드민이면 \`public/admin/{name}/\`). 이름은 사용자에게 확인한다 — 이 이름이 포털 화면에서 고르는 산출물 이름이 된다.
2. \`template.liquid\`를 만든다. 화면 전체를 그리는 마크업이다.
3. 필요하면 \`data.json\`에 **로컬 미리보기용** 샘플 데이터를 넣는다. 실제 값은 플랫폼이 넣는다 — 샘플은 모양을 맞추기 위한 것이지 계약이 아니다.
4. 로컬에서 눈으로 확인한다 — \`bstage dev\`가 liquid 목록·미리보기를 띄운다.
5. 검증한다 — liquid 레포에서 \`bstage build\`가 번들을 만들지 않고 **구조·문법을 검사**한다.
6. 배포는 \`bstage-deploy\` 스킬을 따른다(push된 커밋을 포털이 빌드한다).

> **설치된 cli가 liquid를 모르는 옛 버전이면 4·5가 이 레포를 알아보지 못한다.** 증상이 분명하다 — \`bstage build\`가 liquid 검증 대신 \`src/\` 아래에서 없는 소스 파일을 찾는다고 끝난다. 그때는 cli를 최신으로 올리라고 사용자에게 알린다. 올릴 수 없으면 로컬 미리보기는 **이 레포가 자체로 가진 수단**을 쓰고, 구조·문법은 리뷰로 확인한 뒤 실제 렌더는 첫 배포에서 본다.

## 2. 기존 템플릿 수정

- 고치기 전에 **실제 파일을 읽는다.** 변수 이름과 데이터 모양은 기억이 아니라 그 템플릿과 \`data.json\`에서 확인한다.
- 값이 없을 때 화면이 깨지지 않는지 함께 본다(아래 \`default\` 참고). 실제 데이터에는 샘플에 없던 빈 값이 온다.

# liquid 문법 요점

정확한 태그·필터 목록은 liquidjs 문서가 SSOT다. 자주 쓰는 것만 짚는다.

**변수 출력**

\`\`\`liquid
{{ page.title }}
\`\`\`

이중 중괄호 안의 값이 그 자리에 문자열로 들어간다. 없는 키는 오류가 아니라 **빈 문자열**이 되므로, 오타는 화면이 비는 것으로 나타난다.

**기본값**

\`\`\`liquid
{{ page.title | default: '제목 없음' }}
\`\`\`

값이 없거나 비어 있을 때 대신 쓸 값. 실제 데이터에는 빈 값이 자주 오므로 눈에 보이는 문구에는 기본값을 두는 편이 안전하다.

**조건**

\`\`\`liquid
{% if items.size > 0 %}...{% else %}표시할 항목이 없습니다{% endif %}
\`\`\`

\`{% %}\`는 출력하지 않고 흐름만 제어한다. 빈 목록 분기는 거의 항상 필요하다.

**반복**

\`\`\`liquid
{% for item in items %}{{ item.name }}{% endfor %}
\`\`\`

목록을 순회한다. 안에서 \`forloop.index\` 같은 보조 값을 쓸 수 있다(상세는 liquidjs 문서).

**필터**

\`\`\`liquid
{{ item.price | times: 2 }} / {{ item.body | escape }} / {{ list | size }}
\`\`\`

\`|\`로 값을 변환한다. 사용자 입력이 섞인 값을 HTML로 넣을 때는 이스케이프 여부를 확인한다 — 원시 HTML을 그대로 출력하는 필터는 신뢰할 수 있는 값에만 쓴다.

# 함정

- **sdk와 liquid를 한 레포에 섞지 않는다.** \`template.tsx\`(React)와 \`.liquid\`가 한 레포에 같이 있으면 포털이 빌드하지 못한다. 한 레포는 한 방식만.
- **liquid는 PAGE 배치만 가능하다.** 슬롯(위젯 자리)에는 넣을 수 없다. 포털에서 페이지를 만들 때 커스텀 방식으로 \`liquid\`를 고른다.
- **\`data.json\`은 로컬 전용이다.** 포털이 패키징할 때 지운다. 실제 데이터의 출처가 아니므로 여기 값을 맞춰 두고 "배포하면 이 값이 나온다"고 기대하지 않는다.
- **\`layout.json\`도 지워진다.** 레이아웃은 배치 설정이 소유한다 — 레포에서 정하려 하지 않는다.
- **경로 깊이를 지킨다.** 규약 밖 위치의 파일은 무시된다. "분명 만들었는데 목록에 없다"의 대부분이 이 경우다.
- **에셋(이미지·영상)은 포털 미디어에 올리고 그 URL을 쓴다.** 포털 화면의 스테이지 > 미디어에 파일을 올리면 URL을 받는다. 그 URL을 템플릿에 **절대 주소로** 넣는다. 이미지·영상만 올릴 수 있고, 허용 형식·용량 한도는 업로드할 때 화면이 알려 준다.
- **레포에 넣고 상대 경로로 참조하면 배포 후 깨진다.** 렌더된 HTML이 스페이스 도메인 페이지 안으로 들어가므로 상대 경로는 그 도메인 기준으로 풀리고, 산출물이 놓인 주소를 가리키지 않는다. 옛 가이드에 \`public\`에 두고 상대 경로로 참조하라고 적힌 것이 있는데 신규 포털에서는 맞지 않는다.
- **로컬 프리뷰는 이 점에서 배포와 반대로 동작한다.** 프리뷰는 템플릿 폴더의 파일을 함께 서빙하므로 상대 경로 이미지가 잘 보인다. **프리뷰에서 보였다는 것을 근거로 삼지 않는다.**
- **데이터 모양을 추측하지 않는다.** 최상위 이름은 위 표가 전부이고, 그 안의 필드는 실제 렌더 결과로 확인한다.

# 하지 말 것

- \`public/{user|admin}/{name}/\` 밖에 템플릿 파일 두기
- \`template.liquid\` 외의 파일명 쓰기
- \`data.json\`·\`layout.json\`에 배포에 필요한 설정을 담기 (패키징 때 사라진다)
- 같은 레포에 React 템플릿 추가하기
- 문법·필터를 기억으로 지어내기 — liquidjs 문서에서 확인한다
- 값이 항상 있다고 가정하기 — 빈 값·빈 목록 분기를 함께 넣는다
`
}
